---
title: 备份问题
icon: DatabaseBackup
---

## 自动备份

<Callout type="warn">
如果文件中含有图片，查看备份文件的时候，需要提前把备份文件复制到主文件同一目录下，否则图片无法正常显示。
因为json文件中引用图片是相对路径
</Callout>

### 备份文件如何恢复？

可以将其中一个backup文件直接改后缀名，改回json，将原来的json文件覆盖即可。每个备份文件都有时间，可以挑一个较近的恢复。


---
title: 应用变体
icon: Layers
---

应用变体（Variant）是指在版本号最后添加的标识符

比如 `1.2.3-win7`，其中的 `win7` 就是变体名称

如果是 Nightly Build，变体名称在 Run Number 后面，比如 `0.0.0-nightly.123-win7`

目前支持的变体有：

- `win7`: 向下兼容 Windows 7 SP1 系统
- `foss`: FOSS 版本，如果你不知道这是什么，就不要下载这个

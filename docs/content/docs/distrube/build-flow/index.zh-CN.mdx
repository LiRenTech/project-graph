---
title: 构建流程
icon: Hammer
---

Project Graph 使用 GitHub Actions 进行自动化构建

仓库内有两个用于构建应用的工作流：

- `Nightly Build`: 在每天的 00:00 UTC 运行，或者手动触发
- `Release`: 只能手动触发，需要传入一个参数 `release`，用于指定发布版本号，格式见[版本历史概要](../app/history)

这两个工作流都会调用一个[**基础的工作流** `Publish`](./build-flow/publish-workflow)
